apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: simulator-ingress
  namespace: simulator
spec:
  rules:
  - host: dev.bestcoder.top
    http:
      paths:
      - path: /mock-api
        pathType: Prefix
        backend:
          service:
            name: api-simulator
            port:
              number: 5000
